<div class="bg-white">
    <div class="max-w-screen-xl mx-auto py-24 px-4 sm:px-6 lg:px-8">
        <div class="w-5/6 mx-auto">
            {% if block.galleryTitle %}
                <h2 class="text-light_grey text-3xl text-center lg:text-5xl font-bold mb-8">
                    {{ block.galleryTitle }}
                </h2>
            {% endif %}
            {{ block.galleryIntro | markdown }}
            {% if block.galleryTitle %}
                <div class="mt-12"></div>
            {% endif %}
            <div class="grid grid-cols-1 gap-8 lg:gap-16 relative md:grid-cols-{{ block.itemsPerRow.value }} lg:grid-cols-{{ block.itemsPerRow.value }}">
                {% set images = block.galleryImages.all() %}
                {% for image in images %}
                    <div data-aos-duration="800" data-aos="fade-up" data-aos-delay="{{ loop.index * 200 }}" class="flex justify-center col-span-1">
                        {% if image.linkUrl | length %}
                        <a href="{{ image.linkUrl }}">
                            <img class="" src="{{ image.url }}" alt="{{ image.title }}">
                        </a>
                        {% else %}
                            <img class="" src="{{ image.url }}" alt="{{ image.title }}">
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
</div>